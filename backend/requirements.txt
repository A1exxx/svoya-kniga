# Этап 1 — ядро расчётов и тесты
pytest==8.3.4
httpx==0.28.1  # для FastAPI TestClient

# Backend API (задача #7)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Аккаунты + база данных (серверный этап)
SQLAlchemy==2.0.36
argon2-cffi==23.1.0
email-validator==2.2.0
python-multipart==0.0.20
# Postgres-драйвер для прода (локально не обязателен — по умолчанию SQLite):
# psycopg[binary]==3.2.3

# Формирование XML отчётности (позже, задача формирования декларации)
lxml==5.3.0
